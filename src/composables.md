# composables 技术总结

## 职责定位

`frontend/src/composables/` 负责抽离组合式状态逻辑，当前重点是模块切换状态管理。

## 当前实现

| 文件 | 作用 |
| --- | --- |
| `useModule.ts` | 根据路由与本地缓存维护当前模块，并在切换时跳转默认页面 |

## 技术要点

- 使用 `localStorage` 持久化当前模块，刷新后仍能保留上下文。
- 监听路由自动推断模块，避免页面地址与导航状态不一致。
- 目录规模目前较小，但职责明确，是后续抽出更多共享逻辑的自然位置。
