# layouts 技术总结

## 职责定位

`frontend/src/layouts/` 负责页面外壳布局，把全局头部、侧边栏与业务内容区组织成统一骨架。

## 当前实现

| 文件 | 作用 |
| --- | --- |
| `AppLayout.vue` | 全局布局组件，管理头部、侧边栏折叠和内容区域 |

## 技术要点

- 布局层保存侧边栏折叠状态并持久化到 `localStorage`。
- 通过 `router-view` 承载所有业务页面，属于标准的 SPA 壳层设计。
- 将导航壳与页面内容解耦，便于后续替换页面或新增模块。
